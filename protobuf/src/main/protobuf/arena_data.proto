syntax = "proto3";

package protobuf;

import "cunit.proto";

message ArenaData {
  enum ArenaState {
    GAME_START = 0;

    SETUP = 3;

    ACTIVE = 4;

    CLEANUP = 2;

    GAME_OVER = 1;

    GAME_CANCELED = 5;
  }

  repeated Cunit friends = 1;
  repeated Cunit enemies = 2;
}
